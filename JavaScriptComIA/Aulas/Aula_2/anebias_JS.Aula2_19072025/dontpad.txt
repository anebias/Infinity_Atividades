https://dontpad.com/dfs7071907

drive:
https://drive.google.com/drive/folders/1ACJ6cGv0VVeJy8PejxuCa6X6GMoEuXXB?usp=sharing

drive:
https://drive.google.com/drive/folders/1ACJ6cGv0VVeJy8PejxuCa6X6GMoEuXXB?usp=sharing

https://excalidraw.com/


Enunciado exercício 10:

adicione esses comentários dentro da tag "script"
// Calcule a média das notas do aluno e verifique se o estudante passou de ano.
// O aluno deve passar de ano se a média das suas 4 notas for superior a 7.
// Se a média for igual ou superior a 7, o aluno é aprovado.
// Caso contrário, o aluno é reprovado.




Exercício 9 está incorreto, substituir por:


<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exercícios de Javascript</title>
</head>

<body>
    <h1>Exercícios de Javascript</h1>
    <label for="idade">Digite sua idade:</label>
    <input type="number" name="idade" id="idade"><br>
    <label for="pago">Você pagou a taxa de inscrição? (Digite "sim" ou "não"):</label>
    <input type="text" name="pago" id="pago"><br>
    <p id="resultado"></p>
    <button onclick="action()">Clique aqui para executar a função</button>

    <script>
        function action() {
            // Enunciado:
            // O aluno pode se inscrever para a competição se:
            // 1. A idade do aluno for maior ou igual a 18 anos.
            // 2. O aluno pagou a taxa de inscrição.
            // O aluno pode se inscrever caso ambas as condições sejam verdadeiras.
            // Utilize o operador lógico AND (&&) para verificar ambas as condições.
            
            let idade = parseInt(document.getElementById('idade').value);  // Captura a idade do aluno
            let pago = document.getElementById('pago').value.toLowerCase();  // Captura se pagou a taxa ("sim" ou "não")
            let resultado = '';  // Variável para armazenar o resultado final

            // Escreva aqui a lógica para verificar as condições para inscrição na competição.
            
            document.getElementById('resultado').innerText = resultado;  // Exibe o resultado na página
        }
    </script>
</body>

</html>




DESAFIO:


<!DOCTYPE html>
<html lang="pt">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exercício Desafio</title>
</head>

<body>
    <h1>Desafio: Calculadora de IMC</h1>

    <label for="peso">Digite seu peso (kg):</label>
    <input type="number" name="peso" id="peso" step="0.1"><br>

    <label for="altura">Digite sua altura (m):</label>
    <input type="number" name="altura" id="altura" step="0.01"><br>

    <p id="resultado"></p>

    <button onclick="calcularIMC()">Clique aqui para calcular seu IMC</button>

    <script>
        // Crie a função calcularIMC() que deve:
        // 1. Capturar os valores inseridos pelo usuário nos campos de peso e altura.
        // 2. Calcular o IMC com a fórmula IMC = peso / (altura * altura).
        // 3. Criar uma variável para armazenar o resultado.
        // 4. Se o IMC for:
        //    - Menor que 18.5, exibir "Abaixo do peso".
        //    - Entre 18.5 e 24.9, exibir "Peso normal".
        //    - Entre 25 e 29.9, exibir "Sobrepeso".
        //    - Entre 30 e 39.9, exibir "Obesidade".
        //    - Maior ou igual a 40, exibir "Obesidade mórbida".
        // 5. Exibir a classificação do IMC dentro da tag <p> com id="resultado".
    </script>
</body>

</html>




